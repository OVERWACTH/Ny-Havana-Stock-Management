<form method="post" action="{{ path('app_fiche_de_stock_delete', {'id': fiche_de_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_de_stock.id) }}">
    <button class="btn">Supprimer</button>
</form>
